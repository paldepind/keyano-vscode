# Cursor is denoted by |

# Text objects

- describe: line
  scenarios:
  - it: selects the line at cursor
    start: |
      this is a line
      horse mo|nkey fish
      this is a another line
    input: k
    selection is: "horse monkey fish\n"
  - it: moves backwards
    start: |
      first line
      second line
      third line
      fourth| line
    input: lll
    selection is: "second line\n"

- describe: word
  scenarios:
  - it: selects the word on next
    start: this is a bu|nch of words
    input: ;
    selection is: bunch
  - it: selects the word on previous
    start: this is a bu|nch of words
    input: j
    selection is: bunch
  - it: selects first word in buffer
    start: \|this is a bunch of words
    input: ;
    selection is: this
  - it: expands out to entire word when part of the end is selected
    start: this par[tly] selected
    input: ;
    selection is: partly
        
- describe: parentheses
  scenarios:
  - it: selects next parentheses
    start: |
      the|re is (a parenthesis)
    input: n(
    selection is: (a parenthesis)

# Commands

- describe: select-all
  scenarios:
  - it: selects all parenthesis
    start: |
      he|re (are) some (parenthesis) on (a) line
    input: a(
    selections are: ["(are)", "(parenthesis)", "(a)"]

# Actions

# uncomment when state is reset between tests
# - describe: entering insert mode
#   scenarios:
#   - it: enters insert after selection
#     start: |
#       ther[e is a sele]ction
#     input: ie
#     text is: |
#       theree is a selection
- describe: delete
  scenarios:
  - it: deletes line
    start: |
      this is a line
      horse mo|nkey fish
      this is a another line
    input: kx
    text is: |
      this is a line
      this is a another line